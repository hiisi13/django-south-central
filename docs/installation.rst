============
Installation
============

At the command line::

    $ easy_install south_central

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv south_central
    $ pip install south_central